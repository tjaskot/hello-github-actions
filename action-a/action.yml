name: "Hello Actions"
description: "Greet someone"
author: "tjaskot@github.com"

inputs:
  TJASKOT:
    description: "Who to greet"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
